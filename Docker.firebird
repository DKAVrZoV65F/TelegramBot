# Образ для Firebird, основанный на Ubuntu
FROM ubuntu:22.04

# Обновляем и устанавливаем необходимые пакеты
RUN apt-get update && apt-get install -y \
    libfbclient2 \
    openssh-server \
    # любые другие зависимости, если нужны

# Создаем рабочую директорию
WORKDIR /firebird

# Копируем ваш bin файл в контейнер
COPY your_firebird_bin_file /firebird/

# Можно добавить команду для запуска вручную
CMD ["/bin/bash"]